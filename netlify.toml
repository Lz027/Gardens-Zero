[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NITRO_PRESET = "netlify"
  NODE_VERSION = "22"

[dev]
  command = "npm run dev"
  framework = "#custom"
  targetPort = 8080
  port = 8889
